name: Module Template

description: 
  This is an example module skeleton.

tags:
  - template
  - example

author: Dionysis Athinaios

preview: |
  {
    arg instance; 
    instance.run(\ModuleTemplate, (a: 5, b: 777), local: false);
  }
