name: Clicked Sines

description:
  This is a simple repeating chord or melody using sine waves.

tags:
  - chord
  - iteration
  - pattern
  - sinosc

author: Orestis Karamanlis

example: |
  ( // use your modules instance in place of m
    m.run(
      'ClickedSines',
      (note: [63, 65, 74], pos: Pseq([-1, 0, 1, 0], inf), dur: 1)
    );
  );

preview: |
  {
    arg instance; var sound;
    sound = instance.run(
       'ClickedSines',
       (note: [63, 65, 74], pos: Pseq([-1, 0, 1, 0], inf), dur: 1, global: true));
    { 3.wait; sound.stop; }.fork;
  }
