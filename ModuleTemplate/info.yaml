name: Module Template

description:
  This is an example module skeleton.

tags:
  - template
  - example

author: Dionysis Athinaios

example: |
  {
    arg instance;
    instance.run(\ModuleTemplate, (a: 5, b: 777));
  }

preview: |
  {
    arg modules;
    modules.run(\ModuleTemplate, (a: 5, b: 777, global: true));
  }

